{
	admin off
}

{$DARSM_DOMAIN} {
	encode zstd gzip

	log {
		output stdout
		format json
	}

	request_body {
		max_size 550MB
	}

	reverse_proxy api:8000 {
		health_uri /api/health/ready
		health_interval 15s
		health_timeout 5s
	}

	header -Server
}
